# Complexity naive_heapify

If N is the length of the vector  
  
O(N) = loop in vector  
O(logN) = worst case traversing the tree when bubbling up
  
O(N) * O(logN) = O(N * logN)  

# Complexity performant_heapify 

O(N) + O(N) = O(N)